TO RUN THE APP:
 1 - "docker-compose up --build" command on terminal to build the services (app and db)

    After running the data inserted to the DB is displayed on the terminal (SELECT * FROM trucks -> query)